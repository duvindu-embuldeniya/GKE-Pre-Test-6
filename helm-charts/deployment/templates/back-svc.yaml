apiVersion: v1
kind: Service
metadata:
  name: django-svc
spec:
  type: ClusterIP
  selector:
    app: django
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}